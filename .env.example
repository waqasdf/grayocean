# GrayOcean — copy to .env.local and fill in values from Supabase Dashboard

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# After migration 20260721150000_credit_order_rpcs.sql, set treasury in SQL:
#   update public.app_settings set value = '0x...' where key = 'usdc_receiving_address';
# Network: USDC on Base only. Do not commit real addresses to git.

# Optional: edge function secrets via `supabase secrets set`
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
